server:
  port: 8090

spring:
  application:
    name: spring-ai-mcp-demo
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  datasource:
    url: jdbc:mysql://192.168.6.101:3306/uni_ai_robot?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=GMT%2B8
    username: root
    password: PV8NQ@RSkVHZ

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: false
  ai:
    deepseek:
      # 从环境变量 DEEPSEEK_API_KEY 读取，不写默认值避免提交到仓库。本地可设环境变量或使用 application-local.yml（见 application-local.yml）
      api-key: ${DEEPSEEK_API_KEY:}
      chat:
        options:
          model: deepseek-chat
    mcp:
      server:
        name: incident-mcp-demo
        version: 0.1.0
        # STATELESS：无会话，每个请求独立，适合 Postman 单次请求测试（无需 Session ID）
        protocol: STATELESS

management:
  endpoints:
    web:
      exposure:
        include: "health,info,metrics,prometheus"
